# audio-filter — todo

## 1. Classify exports into subpaths

- [x] Settle on 6 standard, non-overlapping categories
- [ ] Create sub-index files
- [ ] Add subpath entries to `package.json` `"exports"`
- [ ] Update `index.js` category comments

```
audio-filter/weighting → aWeighting, cWeighting, kWeighting, itu468, riaa
audio-filter/auditory  → gammatone, octaveBank, erbBank, barkBank
audio-filter/analog    → moogLadder, diodeLadder, korg35
audio-filter/speech    → formant, vocoder
audio-filter/eq        → graphicEq, parametricEq, crossover, crossfeed
audio-filter/effect    → dcBlocker, comb, allpass, emphasis, deemphasis,
                         resonator, envelope, slewLimiter, noiseShaping,
                         pinkNoise, spectralTilt, variableBandwidth

audio-filter           → all of the above (backward-compat)
```

---

## 2. README

One-pager. Covers everything. No separate docs files.

- [ ] Header: name, one-sentence description, install
- [ ] **Two API shapes** (the essential conceptual split):
  - Coefficient filters: `aWeighting(fs) → SOS[]` — return biquad sections for use with `digital-filter`
  - Processing filters: `moogLadder(data, params)` — mutate Float32/64Array in-place, state in `params._s`
- [ ] **Block-processing pattern** (the hidden API — state lives in params, reuse across buffers):
  ```js
  let params = { fc: 1000, resonance: 0.5 }
  for (let buf of stream) moogLadder(buf, params)  // state preserved automatically
  ```
- [ ] Filter table: category | name | type (SOS/in-place) | key params | reference
- [ ] Frequency response images (one per filter, generated by plots script)
- [ ] Import examples for each subpath

---

## 3. Frequency response plots

Adapt `~/projects/digital-filter/plots/generate.js` to drive `audio-filter` filters.
Each filter gets a 4-panel SVG: magnitude (dB), phase (°), group delay, impulse response.

- [ ] Copy/adapt `generate.js` → `plots/generate.js`
- [ ] Write plot spec for each filter (params to use, frequency range, y-axis range)
  - Weighting: aWeighting, cWeighting, kWeighting, itu468, riaa — all on same axes for comparison
  - Auditory: gammatone (multiple fc), octaveBank, erbBank, barkBank — bank overlays
  - Analog: moogLadder (sweep resonance), diodeLadder, korg35 (LP+HP)
  - Speech: formant, vocoder
  - EQ: graphicEq, parametricEq, crossover (multi-band overlay), crossfeed
  - Effect: dcBlocker, comb, allpass (1st+2nd), resonator, spectralTilt, variableBandwidth
- [ ] Output SVGs to `plots/` folder
- [ ] Embed in README (one image per filter)

---

## 4. TypeScript types

Minimal `.d.ts` — covers the two API shapes, no over-engineering.

- [ ] `index.d.ts`: type for processing filters `(data: Float32Array | Float64Array, params: object) => Float32Array | Float64Array`
- [ ] Typed params per filter (at minimum: `fc`, `fs`, `resonance`, `Q`, `gain`)
- [ ] Add `"types"` field to `package.json`

---

## 5. Demo

Interactive browser page. Filter picker → frequency response chart + audio preview.

- [ ] Filter selector (grouped by category)
- [ ] Frequency response chart (canvas or SVG, real-time as params change)
- [ ] Param sliders (fc, resonance/Q, gain — filter-specific)
- [ ] Test audio: one-button playback through filter on sample file
- [ ] Drop any file → apply filter → download
- [ ] Waveform display via wavefont player
